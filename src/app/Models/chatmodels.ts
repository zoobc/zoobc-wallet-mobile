export interface User {
    email: string;
    address: string;
    name: string;
    time: string;
}

export interface Chat {
    message: string;
    pair: string;
    sender: string;
    time: any;
}
