import { CurrencySelectorDirective } from './currency-selector.directive';

describe('CurrencySelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencySelectorDirective();
    expect(directive).toBeTruthy();
  });
});
