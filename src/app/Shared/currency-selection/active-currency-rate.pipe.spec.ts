import { ActiveCurrencyRatePipe } from './active-currency-rate.pipe';

describe('ActiveCurrencyRatePipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveCurrencyRatePipe();
    expect(pipe).toBeTruthy();
  });
});
