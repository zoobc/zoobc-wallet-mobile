import { AddressElipsisDirective } from './address-elipsis.directive';

describe('AddressElipsisDirective', () => {
  it('should create an instance', () => {
    const directive = new AddressElipsisDirective();
    expect(directive).toBeTruthy();
  });
});
